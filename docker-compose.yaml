version: '3.8'

services:
  frontend:
    image: nginx:alpine
    expose:
      - "80"
    restart: always

  backend:
    image: python:3.10-alpine
    command: python -m http.server 8000
    expose:
      - "8000"
    restart: always
